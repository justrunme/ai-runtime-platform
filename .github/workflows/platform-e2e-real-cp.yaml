name: Platform e2e (real Control Plane)

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  real-control-plane:
    uses: ./.github/workflows/platform-e2e-real-cp-reusable.yaml
    with:
      control_plane_image: ghcr.io/justrunme/ai-infra-control-plane:1.3.0
